"""Deterministic, explainable risk engine.

Risk is a transparent signal ledger + a versioned policy — no random scoring and
no AI black box. Signal calculators are pure functions; the policy turns the
aggregated signals into an explainable decision with reason codes.
"""
